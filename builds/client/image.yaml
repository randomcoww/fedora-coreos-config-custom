# include: nvidia-gpu-image.yaml

size: 5

extra-kargs:
  - elevator=noop
  - rd.neednet=1
  - ip=dhcp
  - enforcing=0
  - coreos.no_persist_ip
  - rfkill.default_state=1

ignition-network-kcmdline: []
ostree-remote: fedora
sysroot-readonly: true